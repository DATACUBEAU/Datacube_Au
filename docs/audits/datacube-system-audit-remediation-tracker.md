# DataCube AU System Audit Remediation Tracker

Status model:

- Red = confirmed risk, not fixed
- Yellow = partially fixed or needs staging verification
- Green = fixed and verified
- Grey = not verified from repository alone

Last updated: 2026-07-29

| # | Audit item | Status | Evidence / file paths | Changed in this pass | What remains | Recommended next sprint | Verification result |
|---:|---|---|---|---|---|---|---|
| 1 | VPS shared-secret fallback | Green | `src/app/api/au/vps-ticket/route.ts`, `src/lib/server/vps-ticket-config.ts`, `vps-ai-gateway/src/auth.ts`, `vps-ai-gateway/src/index.ts`, JS companions | Removed hardcoded production fallback. Missing secret now fails closed unless explicit local-dev flag is set outside production. | Deployment gate: staging must confirm `VPS_SHARED_SECRET` is configured on both Next.js and VPS before deploy. | Secret rotation/runbook after coordinated deployment window. | Verified by `tests/vps-ticket-security.unit.test.ts`, `vps-ai-gateway` build, root typecheck, and root production build. |
| 2 | VPS ticket not bound to feature/route | Green | `src/lib/server/vps-ticket-config.ts`, `vps-ai-gateway/src/auth.ts`, `vps-ai-gateway/src/index.ts` | Tickets now carry `feature`, `feature_key`, `route`, `ticket_id`, issuer, audience, and expiry. VPS rejects route/feature mismatch. | Staging should test all AI UI flows against the deployed gateway. | Add end-to-end browser-to-VPS route-binding checks. | Verified by focused tests for accepted routes, wrong route, wrong feature, missing feature, expired, and tampered tickets. |
| 3 | Full Supabase document hydration before generation | Green | `vps-ai-gateway/src/generation-handler.ts`, `vps-ai-gateway/src/chat-handler.ts`, `src/lib/api/documents.ts`, `vps-ai-gateway/src/retrieval-service.ts` | Removed `content_text` hydration from Supabase. Replaced with server-side Qdrant `RetrievalService` with explicit `semanticTopKRetrieval` and `boundedCoverageRetrieval` bounded chunk retrieval modes. Limited `getDocumentText` to 15 chunks (12000 chars) for client preview limits. | Supabase still serves auth, metadata, and a strict fallback. | Consolidate any remaining minor full-text fallbacks if found in edge cases. | Verified by code replacing `.select('content_text')` and adding tenant-safe retrieval and passing tests. |
| 4 | Supabase authenticated GET caching in PWA/service worker | Green | `next.config.ts`, `shared/pwa-runtime.js`, `worker/index.js`, `public/sw.js`, `public/worker-5ce8b32c710b2d02.js` | Supabase GET runtime rule changed to `NetworkOnly`; runtime cache version bumped; SW fallback warnings no longer serialize request headers; build regenerated SW artifacts. | Staging should still verify old runtime caches purge on real browser clients. | Add browser-level service-worker cache assertion in CI. | Verified by `tests/security-hotfix-static.unit.test.ts`, `npm run test:pwa-offline`, and production build. |
| 5 | Offline queue persisting Authorization headers | Green | `src/lib/offline/write-queue.ts`, `src/lib/offline/sync-engine.ts`, `src/lib/api/safe-fetch.ts`, `src/lib/api/documents.ts`, `src/lib/auth/session-storage.ts`, `src/lib/offline/db.ts` | Queue sanitizes sensitive headers, scopes private writes to user, reacquires auth at replay time, rejects user mismatch, clears private queue on logout/account switch, and DB v2 scrubs legacy queued headers. | Staging should verify queued deletes across real login/logout/account switch flows. | Add browser-level IndexedDB replay tests. | Verified by `tests/offline-queue-security.unit.test.ts`, root typecheck, and production build. |
| 6 | Atomic usage accounting | Yellow | `supabase/migrations/20260728153000_atomic_usage_accounting.sql`, `supabase/migrations/20260728154500_atomic_usage_limit_scope_fix.sql`, `supabase/migrations/20260728160000_atomic_usage_replay_guard.sql`, `src/app/api/au/vps-ticket/route.ts`, `src/lib/server/ai-usage-accounting.ts`, `vps-ai-gateway/src/usage-accounting.ts`, `vps-ai-gateway/src/chat-handler.ts`, `vps-ai-gateway/src/generation-handler.ts` | Replaced pre-charge ticket issuance with server-side reserve -> gateway begin -> commit/release lifecycle. Reservation mutation is service-role only, deduped by `(user_id, feature_key, idempotency_key)`, backed by row locks on usage counters, checks daily quotas against daily counters, rejects payload-swapped idempotency reuse, and blocks duplicate provider starts for active reservations. | Oracle VPS/frontend/RAG worker redeploy and live streaming retry tests remain. Expiry cleanup needs to be scheduled. | Provider usage reconciliation and budget dashboards. | Verified by `npm run test:atomic-usage`, `tests/vps-ticket-security.unit.test.ts`, root typecheck/build, VPS build, Supabase pushes, post-push migration list, and final dry run reporting remote up to date; live retry/reconnect tests still pending. |
| 7 | Admin provider API key exposure | Yellow | `src/app/api/admin/handler/route.ts`, `src/app/conex/page.tsx`, `src/lib/api/admin-fetch.ts`, `src/lib/server/provider-key-encryption.ts`, `supabase/migrations/20260728120000_api_key_pipeline_hardening.sql`, `supabase/migrations/20260729120000_provider_key_encryption_columns.sql` | Provider keys are selected through masked metadata for admin DTOs, create/update no longer echoes raw values, new create/update writes encrypted provider-key values, revoke clears plaintext and encrypted fields, legacy admin token localStorage/header flow was removed, and provider key audit/encryption metadata migrations are applied remotely. The owner reports exposed credentials have been rotated. | Deploy the code, set `PROVIDER_KEY_ENCRYPTION_SECRET` server-side, re-enter/rotate any legacy plaintext provider rows so `key_value` can remain null, and smoke-test live provider use. | Add per-use audit events and remove plaintext fallback after all legacy rows are rotated. | Covered by `tests/security-hotfix-static.unit.test.ts` and `tests/provider-key-storage.unit.test.ts`; user-confirmed final Supabase dry run is up to date; live provider-use integration remains. |
| 8 | Qdrant tenant-filter hardening | Green | `vps-ai-gateway/src/retrieval-service.ts`, `vps-ai-gateway/tests/retrieval-service.unit.test.ts` | Implemented strict `must` match filters for `user_id` and `document_id`. Missing tenant filter gracefully fails closed and zero-results out. Cross-user access is explicitly denied. | Qdrant tenant hardening complete for AI Generation. | Consolidate any soft fallbacks in admin routes if they exist. | Tests `tests/retrieval-service.unit.test.ts` pass proving failure when tenant filters are omitted or crossed. |
| 9 | AI routing split between Next.js and VPS | Yellow | `shared/ai-gateway-contract.cjs`, `shared/ai-gateway-contract.ts`, `src/lib/server/vps-ticket-config.ts`, `vps-ai-gateway/src/auth.ts`, `vps-ai-gateway/src/ai-routing.ts`, `src/lib/server/ai-routing.ts` | Added a shared gateway contract for feature aliases, route binding, request types, paid-plan defaults, and hidden/internal model rejection. Next.js ticket issuance and VPS route verification now resolve from the same contract. | The VPS still sources provider credentials from env while Next.js can use database provider-key routing; live gateway tests must confirm no drift after deployment. | Consider a single provider registry service after the contract is proven stable. | Covered by `tests/ai-routing-contract.unit.test.ts`; live route/model/provider smoke tests pending. |
| 10 | Migration/schema drift | Green | `supabase/migrations/20260728120000_api_key_pipeline_hardening.sql`, `supabase/migrations/20260728153000_atomic_usage_accounting.sql`, `supabase/migrations/20260728154500_atomic_usage_limit_scope_fix.sql`, `supabase/migrations/20260728160000_atomic_usage_replay_guard.sql`, `supabase/migrations/20260729120000_provider_key_encryption_columns.sql`, `supabase/migrations/20260729153000_username_uniqueness.sql` | User-confirmed Supabase migration list includes the hardening, atomic usage, provider-key encryption, and username uniqueness migrations; final dry run reports the remote database is up to date. | Broader remote policy/catalog diff can wait for a separate audit; no known hardening migration drift remains. | Pick one canonical migration root later if legacy backend migration history still matters. | Verified by user-run Supabase CLI final dry run: remote database is up to date. |
| 11 | Worker repeated file downloads | Yellow | `rag-worker/src/source-cleanup.ts`, `rag-worker/src/worker.ts`, `rag-worker/src/cleanup.ts` | Completed ingestion now has owner/path-bound source deletion and bounded cleanup retry, reducing retained source files and future repeated downloads. | Full duplicate-job prevention and extraction cache are still not implemented. | Add duplicate-job guards and chunk/vector reuse checks. | Source cleanup tests and worker lifecycle tests passed; broader duplicate-job risk remains. |
| 12 | Sensitive debug/token/document logging | Yellow | `src/app/api/proxy/_supabase-auth.ts`, `next.config.ts`, `worker/index.js`, `rag-worker/src/worker.ts`, `rag-worker/src/ingestion.ts`, `src/app/api/admin/handler/route.ts`, `src/hooks/use-smart-auth.tsx`, `src/lib/api/safe-fetch.ts`, `src/lib/supabase-client/client.ts`, `src/components/global-listeners.tsx` | Removed token previews, request-header snapshots, extracted-text preview, chunk text preview; admin debug details are redacted. Auth/session/safeFetch logs are now development/debug gated and avoid session/token/error-body values. | Oracle VPS, frontend server, RAG worker, and browser live logs must be checked after deployment. | Central redaction utility and log policy tests. | Static sweep and focused auth/session tests passed for targeted patterns; live logs pending. |
| 13 | Original uploaded file retention after successful ingestion | Yellow | `rag-worker/src/source-cleanup.ts`, `rag-worker/src/worker.ts`, `rag-worker/src/cleanup.ts`, `rag-worker/src/repair.ts`, `supabase/migrations/20260309110000_source_cleanup_tracking.sql` | Source cleanup is invoked after successful worker completion, verifies completed status, owner, and exact path, treats already-missing as idempotent success, and records bounded failure state. | Staging must verify real Storage deletion with service-role access and confirm any legacy `backend/rag-worker` copy is not deployed. | Remove/retire legacy worker copy or mirror changes if it is still used. | Verified by `rag-worker/tests/source-cleanup.test.ts`, `rag-worker/tests/worker-lifecycle.test.ts`, and worker build; live Storage deletion remains staging-only. |
| 14 | API key exposure pipeline | Yellow | `docs/security/datacube-au-api-key-pipeline-audit.md`, historical backend scripts/migrations, `labeled_understanding.md` | Confirmed tracked historical credential values were removed/replaced with placeholders; `.temp` files were removed from git tracking; ad hoc migration helper scripts were deleted; unsafe public provider-key guidance was corrected. The owner reports exposed credentials have now been rotated. | Post-rotation live smoke tests and log checks must pass before this can be Green. | Add secret scanning to CI with path-only failure output. | Static scan reports no high-confidence raw secret values in tracked files; live post-rotation validation pending. |
| 15 | Admin credential table protection | Yellow | `src/app/api/admin/handler/route.ts`, `src/app/api/admin/auth/route.ts`, `src/lib/server/provider-key-encryption.ts`, `supabase/migrations/20260728120000_api_key_pipeline_hardening.sql`, `supabase/migrations/20260729120000_provider_key_encryption_columns.sql` | Removed broad credential table responses, added explicit admin-auth log columns, upstream payload sanitization, provider metadata columns, revocation metadata, audit log table, and app-layer encrypted provider-key columns/code. The provider-key encryption migration is applied remotely. New provider-key create/update stores encrypted values and returns masked DTOs only. | Deploy the code, configure `PROVIDER_KEY_ENCRYPTION_SECRET`, re-enter any legacy provider rows that still have plaintext `key_value`, and perform live admin-provider tests. | Remove plaintext fallback after re-entry and verify policies with direct SQL/catalog access. | Static tests cover no broad admin credential selects, masked DTO behavior, and encrypted provider-key storage behavior; user-confirmed final Supabase dry run is up to date. |
| 16 | Provider key masking | Green | `src/app/api/admin/handler/route.ts`, `src/app/conex/page.tsx`, `tests/security-hotfix-static.unit.test.ts` | Admin UI receives configured/not configured, last4/fingerprint, provider/service, status, and timestamps only. | Live admin update flow should be smoke-tested after deployment. | Add API route integration tests with mocked Supabase. | Static test asserts no raw provider-key response path. |
| 17 | Secret logging | Yellow | `vps-ai-gateway/src/utils.ts`, `vps-ai-gateway/src/index.ts`, `src/app/api/admin/auth/route.ts`, `src/app/api/admin/handler/route.ts`, `src/app/api/au/vps-ticket/route.ts`, `vps-ai-gateway/src/usage-accounting.ts` | Added redaction/sanitized provider errors and stopped token/secret/edge-response dumps. Usage accounting errors are reported by code/status only. | Oracle VPS live logs must be checked after restart. | Centralize redaction across all server routes. | Static and code review verification; live logs pending. |
| 18 | Service-role key handling | Yellow | `src/lib/server/supabase-admin.ts`, `src/app/api/admin/users/route.ts`, `src/app/conex/users/route.ts`, backend cleanup files | Service-role use remains server-only in active code; confirmed tracked historical exposure was removed; owner reports the exposed credential has now been rotated. | Post-rotation live smoke tests must confirm frontend/server, Oracle VPS gateway, RAG worker, and cron/background workers all use the new server-only value. | Add pre-commit/CI secret scanner. | High-confidence static scan passes after cleanup; live post-rotation validation pending. |
| 19 | Oracle VPS secret deployment | Yellow | `docs/deployment/oracle-vps-gateway-update.md`, `vps-ai-gateway/.env.example`, `vps-ai-gateway/src/index.ts` | Documented required env vars, CORS allowlist, HTTPS Qdrant expectation, service restart commands, network checks, and live tests. Atomic usage now requires `SUPABASE_SERVICE_ROLE_KEY` on the gateway. | Apply env changes/restart on Oracle VPS and run live tests. | Add deployment smoke-test script. | Documentation complete; live Oracle validation pending. |
| 20 | Production auth expiry and UI freeze recovery | Yellow | `src/lib/auth/redirects.ts`, `src/app/session-expired/page.tsx`, `src/components/auth-lock-overlay.tsx`, `src/lib/supabase-client/client.ts`, `src/lib/api/safe-fetch.ts`, `src/hooks/use-smart-auth.tsx`, `tests/session-expired-reauth.unit.test.ts`, `tests/ui-freeze-prevention.unit.test.ts` | Added a public session-expired page, safe local return-path validation, bounded browser session resolution, single-flight session restore, debug-gated logs, and redirect-based reauthentication instead of refresh/reload loops. Loading overlays no longer inert-lock the app shell. | Deploy and live-test expired sessions, slow account/billing/API responses, stale service-worker clients, and protected route recovery. | Browser-level auth-expiry E2E tests. | Focused static/unit checks passed locally; production expiry behavior needs live verification. |
| 21 | Data security notice and document retention enforcement | Yellow | `src/lib/server/retention-policy.ts`, `src/lib/server/retention.ts`, `src/app/api/cron/retention/route.ts`, `src/app/api/au/document-upload/route.ts`, `src/lib/plans/subscription-policy.ts`, `src/components/inactivity-policy-banner.tsx`, `src/app/dashboard/documents/page.tsx`, `src/app/dashboard/settings/page.tsx`, `tests/retention-policy.unit.test.ts`, `tests/retention-enforcement.unit.test.ts` | Server policy now evaluates earliest of seven-day verified inactivity and plan expiry, upload initiation stores plan-based expiry, the protected cron path supports aggregate dry-run, scheduled cleanup is document-only and owner-bound, and cleanup covers Storage, metadata, chunks, generated artifacts, and Qdrant vectors. The exact notice appears in signup, documents, and settings surfaces. | Production cron must be configured with server-only auth and live-tested. The active migration tree does not define the optional Node cleanup action/run tables or lease RPCs, so live schema verification or an owner-approved migration may still be needed before marking Green. | Add owner-approved retention schema/lease migration if live schema lacks those objects. | Focused retention policy/enforcement tests passed locally; live Storage/Qdrant deletion and cron lease verification pending. |
| 22 | AU onboarding persistence and non-annoying behavior | Green | `src/components/au-assistant.tsx`, `src/components/site-manual-guide.tsx`, `tests/onboarding-responsive.unit.test.ts` | Replaced automatic tour behavior with a dismissible first-time offer, versioned user-scoped state, seven-day Maybe Later cooldown, completed/skipped suppression, auth/expired-route suppression, manual Help -> Take a product tour restart, and mobile-safe assistant dimensions. | Cross-device persistence currently uses user-scoped browser storage; server-backed onboarding sync can wait unless product analytics require it. | Optional server-backed onboarding preferences. | Static tests cover major route copy, cooldown, skip/completed suppression, manual restart, auth-route suppression, and mobile-safe layout. |
| 23 | Mobile PWA install control | Green | `src/components/header-pwa-install-button.tsx`, `src/components/pwa-install-button.tsx`, `src/components/pwa-sidebar-install-button.tsx`, `src/app/dashboard/dashboard-client-layout.tsx`, `tests/mobile-pwa-install.unit.test.ts` | Mobile dashboard header now includes an accessible install control, desktop controls remain, standalone mode hides install UI, iOS shows dismissible Add to Home Screen guidance, and install prompts only fire on user click. | Live iOS/Android install prompt behavior should be smoke-tested after deploy. | Browser-device install prompt E2E checks. | Static tests cover mobile visibility, desktop preservation, no fake prompt, iOS dismissal, standalone hiding, safe-area sizing, and service-worker lifecycle safety. |
