# Skipped Migrations

These files were skipped by the Supabase CLI because they do not follow the `<timestamp>_name.sql` pattern.
They are preserved for historical reference.
They must not be applied automatically.
Any needed SQL from them should be recreated in a new timestamped migration.
